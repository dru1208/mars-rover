const { expect } = require("chai");
const GridOutputProcessor = require("../grid-output-processor.js");

describe("Grid Output Processor", () => {
  it ("should exist", () => {
    expect(GridOutputProcessor).to.not.be.undefined;
  })
})