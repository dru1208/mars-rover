const { expect } = require("chai");
const OutputProcessor = require("../grid-output-processor.js");

describe("Output Processor", () => {
  it ("should exist", () => {
    expect(GridOutputProcessor).to.not.be.undefined;
  })
})

// Output Method

describe("#")